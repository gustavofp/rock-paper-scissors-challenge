import GameInfo from "../GameInfo";
import GameBoard from "../GameBoard";

const Game = () => {
  return (
    <section className="game">
      <GameInfo />
      <GameBoard />
    </section>
  );
};

export default Game;
